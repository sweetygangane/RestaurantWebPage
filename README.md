# RestaurantWebPage
I used Flexbox for layout, media queries for responsiveness, pseudo-classes for hover effects, transitions for smooth animations, and different CSS selectors like class, element, and descendant selectors to style the components.
Selectors (universal, class, element, descendant)
Flexbox layout
Pseudo-classes & pseudo-elements
Media queries (responsive design)
Transitions & transforms
Box model (margin, padding)
Background gradients
